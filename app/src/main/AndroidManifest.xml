<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="id.co.gits.gitsplayground">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".deeplinkbasic.DeeplinkBasicActivity"
            android:parentActivityName=".main.MainActivity" />
        <activity android:name=".deeplinkbasic.DeeplinkActivity">
            <intent-filter
                android:autoVerify="true"
                android:priority="1"
                tools:targetApi="m">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="testdeeplink.com"
                    android:pathPrefix="/lorem"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        <activity android:name=".cordinatorlayoutoverlappingbasic.CoorOverlappingViewActivity"></activity>
    </application>

</manifest>